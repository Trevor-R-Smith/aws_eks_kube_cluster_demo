config {

  application_name = "${application_name}"
  backendUrl = "${backendUrl}"
  kubernetes_version = "${kubernetes_version}"
  apikey = "${apiKey}"
  frontend_config_version = "${frontend_config_version}"
  https = "${https}"

}